id: project.transfer_ownership
source_file: apis/project.yaml
operation_id: transfer_project_ownership_endpoint_projects__project_id__owner_patch
path: /projects/{project_id}/owner
method: PATCH
summary: 프로젝트 소유권 이전
capability: 프로젝트 소유권을 다른 사용자에게 이전
usable_in:
- command
operation_kind: write
when_to_use:
- 사용자가 프로젝트 소유권을 다른 사용자에게 넘기고자 할 때 사용합니다.
when_not_to_use:
- 일반 멤버 추가나 제거 요청에는 사용하지 않습니다.
requires_confirmation: true
risk_level: medium
side_effects:
- Transfer Project Ownership Endpoint
required_headers:
- X-User-Id
- X-User-Role
required_inputs:
  headers:
  - name: X-User-Id
    required: true
    type: string
    description: 사용자 식별자
  - name: X-User-Role
    required: true
    type: string
    description: 사용자 역할
  path:
  - name: project_id
    required: true
    type: integer
  query: []
  body:
    required: true
    content_type: application/json
    schema_ref: '#/components/schemas/ProjectOwnerTransfer'
    type: object
    required_fields:
    - target_user_id
important_inputs:
  path:
  - project_id
  body:
  - target_user_id
preconditions:
- 필수 입력값이 모두 준비되어 있어야 합니다.
missing_info_questions:
- 대상 프로젝트를 확인해야 합니다.
- 새 소유자 닉네임을 확인해야 합니다.
response_interpretation: 소유권 이전 결과를 사용자에게 자연어로 요약합니다.
plan_template:
- 대상 프로젝트를 확인합니다.
- 새 소유자를 확인합니다.
- 프로젝트 소유권을 이전한 뒤 결과를 요약합니다.
examples:
- demo 프로젝트 소유권을 alice에게 넘겨줘
enabled: true
