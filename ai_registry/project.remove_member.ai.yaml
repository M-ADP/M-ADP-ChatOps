id: project.remove_member
source_file: apis/project.yaml
operation_id: remove_project_member_endpoint_projects__project_id__members__target_user_id__delete
path: /projects/{project_id}/members/{target_user_id}
method: DELETE
summary: 프로젝트 멤버 제거
capability: 프로젝트에서 사용자를 멤버에서 제거
usable_in:
- command
operation_kind: delete
when_to_use:
- 사용자가 특정 프로젝트에서 멤버를 제거해달라고 요청할 때 사용합니다.
when_not_to_use:
- 멤버 추가나 소유권 이전 요청에는 사용하지 않습니다.
requires_confirmation: true
risk_level: high
side_effects:
- Remove Project Member Endpoint
required_headers:
- X-User-Id
- X-User-Role
required_inputs:
  headers:
  - name: X-User-Id
    required: true
    type: string
    description: 사용자 식별자
  - name: X-User-Role
    required: true
    type: string
    description: 사용자 역할
  path:
  - name: project_id
    required: true
    type: integer
  - name: target_user_id
    required: true
    type: integer
  query: []
  body: null
important_inputs:
  path:
  - project_id
  - target_user_id
preconditions:
- 필수 입력값이 모두 준비되어 있어야 합니다.
missing_info_questions:
- 대상 프로젝트를 확인해야 합니다.
- 제거할 사용자 닉네임을 확인해야 합니다.
response_interpretation: 멤버 제거 결과를 사용자에게 자연어로 요약합니다.
plan_template:
- 대상 프로젝트를 확인합니다.
- 제거할 사용자를 확인합니다.
- 프로젝트에서 멤버를 제거한 뒤 결과를 요약합니다.
examples:
- demo 프로젝트에서 alice 멤버 제거해줘
enabled: true
