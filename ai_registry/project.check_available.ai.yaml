id: project.check_available
source_file: apis/project.yaml
operation_id: check_project_available_endpoint_projects_available_get
path: /projects/available
method: GET
summary: Check Project Available Endpoint
capability: Check Project Available Endpoint
usable_in:
- query
operation_kind: read
when_to_use:
- 사용자 요청이 'Check Project Available Endpoint'에 해당하는 작업일 때 사용합니다.
when_not_to_use:
- 다른 리소스를 조회하거나 변경하는 요청이면 사용하지 않습니다.
requires_confirmation: false
risk_level: low
side_effects: []
required_headers:
- X-User-Id
- X-User-Role
required_inputs:
  headers:
  - name: X-User-Id
    required: true
    type: string
    description: 사용자 식별자
  - name: X-User-Role
    required: true
    type: string
    description: 사용자 역할
  path: []
  query:
  - name: project_id
    required: true
    type: integer
    description: 확인할 프로젝트 ID
  body: null
preconditions:
- 필수 입력값이 모두 준비되어 있어야 합니다.
missing_info_questions:
- X-User-Id 값을 확인해야 합니다.
- X-User-Role 값을 확인해야 합니다.
- project_id 값을 확인해야 합니다.
response_interpretation: 응답 데이터를 현재 상태 요약과 핵심 수치 중심으로 정리합니다.
plan_template: []
examples:
- Check Project Available Endpoint
