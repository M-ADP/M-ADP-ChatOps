id: project.add_member
source_file: apis/project.yaml
operation_id: add_project_member_endpoint_projects__project_id__members_post
path: /projects/{project_id}/members
method: POST
summary: 프로젝트 멤버 추가
capability: 프로젝트에 사용자를 멤버로 추가
usable_in:
- command
operation_kind: write
when_to_use:
- 사용자가 특정 프로젝트에 멤버를 추가해달라고 요청할 때 사용합니다.
when_not_to_use:
- 멤버 조회나 멤버 제거 요청에는 사용하지 않습니다.
requires_confirmation: true
risk_level: medium
side_effects:
- Add Project Member Endpoint
required_headers:
- X-User-Id
- X-User-Role
required_inputs:
  headers:
  - name: X-User-Id
    required: true
    type: string
    description: 사용자 식별자
  - name: X-User-Role
    required: true
    type: string
    description: 사용자 역할
  path:
  - name: project_id
    required: true
    type: integer
  query: []
  body:
    required: true
    content_type: application/json
    schema_ref: '#/components/schemas/ProjectMemberAdd'
    type: object
    required_fields:
    - user_id
important_inputs:
  path:
  - project_id
  body:
  - user_id
preconditions:
- 필수 입력값이 모두 준비되어 있어야 합니다.
missing_info_questions:
- 대상 프로젝트를 확인해야 합니다.
- 추가할 사용자 닉네임을 확인해야 합니다.
response_interpretation: 멤버 추가 결과를 사용자에게 자연어로 요약합니다.
plan_template:
- 대상 프로젝트를 확인합니다.
- 추가할 사용자를 확인합니다.
- 프로젝트에 멤버를 추가한 뒤 결과를 요약합니다.
examples:
- demo 프로젝트에 alice 멤버 추가해줘
enabled: true
